[package]
name = "mix-cli"
version = "0.1.0"
edition = "2024"

[[bin]]
name = "mix"
path = "src/main.rs"

[dependencies]
clap = { version = "4", features = ["derive"] }
dumpster = "*"
mix = { path = ".." }
serde_json = "1"
