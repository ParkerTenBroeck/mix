{
    /*
    list = [ 1+2, "heelo" ],
    meow.beep = "nya",
    meow.bop = "nya",
    */
    /*
    wow = {
        y = x+1,
        z = y+2,
        x = 1,
    }, */
/*
    
    value2 = "meow",
    value = 12.2,
 

    wow = {wow2 = wow },

    meow = {

        y = x+1,
        x = 1+wow33.x,
    },


    wow33 = {
        y = x+1,
        z = y+2,
        x = 1,
    },

    # inc = x : x+1,

    # two = inc <| inc (inc 1) |> inc,
    
    fib = x : if x <= 0 then 0 else if x == 1 then 1 else fib (x-1) + fib (x-2),

    
    func = final : {x = 1, y = 1+final.x, z = 1+final.y },
    x = func x;



    value_str = "value",
    meow = {
        value = { bwap = "hello" }
    },

    result = meow.${value_str}.bwap,
    value = fib 10,

   */
    fib = (x : if x <= 0 then 0 else if x == 1 then 1 else fib (x-1) + fib (x-2)),

    fib20 = fib 35;


    add = x: y: x + y;
    addOne = add 1;

    meowmeowmeow = add (add 2 3)  (addOne 5);

   test = {x}: false;
   
   faalala = test {x=1/0};

    func = final : {x = 1, y = 1+final.x, z = 1+final.y };
    x = func x;

    meow = foo@[{a=[.., _, _, _],b,c}, x :: int, y] :: Meow : bar;
    recursive = [recursive];

    foo = {
        meow = 1 . 2;
        bwa  = foo: 1,
        bwah = foo: 1,
    },
    list = [1.2, [2], [44]];
    number = list.${list.len - 1}.0;
    
}
